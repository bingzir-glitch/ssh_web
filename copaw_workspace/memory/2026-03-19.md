# 2026-03-19 操作记录

## HEARTBEAT 输出格式调整
- 已修改 `/root/.copaw/workspaces/default/HEARTBEAT.md` 的“步骤 4：最终输出”。
- 输出前缀从仅日期改为“日期+时分秒”：`YYYY-MM-DD HH:MM:SS`。
- 两种固定输出同步更新：
  - `YYYY-MM-DD HH:MM:SS已写入 memory:...`
  - `YYYY-MM-DD HH:MM:SS巡检完成，当前无新变更，无需记录。`

## 本地 Whisper 安装与磁盘清理结论
- 本地 `openai-whisper` 安装未完成：
  - 初次失败原因：`/root/.copaw/venv/bin/python` 无 `pip`（后通过 `ensurepip` 补齐）。
  - 后续失败原因：安装 `torch` 过程中磁盘不足（`No space left on device`）。
- 用户决策：暂停本地 Whisper 安装，先做磁盘/缓存清理。
- 已执行清理（安全项）：
  - 删除 `/root/.cache/uv`、`/root/.cache/pip`、`/root/.cache/huggingface`、`/root/.cache/ms-playwright`、`/root/.cache/node`
  - `apt-get clean`
  - 清理 `/var/log` 大日志与旧轮转日志
  - 清理 npm 缓存（`/root/.npm/_cacache`、`/root/.npm/_logs`）
- 空间变化：根分区可用空间由约 `1.4G` 提升到约 `3.7G`（使用率约 `85% -> 61%`）。

## ark_t2i 文档本地化与精简
- 已改写 `active_skills/ark_t2i/SKILL.md`：
  - frontmatter `description` 改为中文。
  - 正文保留“用途/环境变量配置/使用示例/脚本说明”。
  - 删除话术性与非核心内容（如“方案 A”等描述）。

## 图片在不同渠道的输出规范确认
- 规则固定：
  - Console：使用 Markdown 图片语法 `![](https://...)` 才会内联显示。
  - QQ：使用文本格式 `[Image: https://...]`。
- 备注：纯链接在 Console 中通常只显示文本，不会直接渲染图片。

## ark_t2i 增加“自拍预设”改造并实测出图
- 已修改 `/root/.copaw/workspaces/default/active_skills/ark_t2i/scripts/generate.py`：
  - 新增参数：`--preset selfie_realistic`、`--subject`、`--scene`、`--mood`、`--channel`。
  - 新增自拍模板拼装逻辑（写实插画自拍，前置镜头视角、自然肤质、生活化光线）。
  - JSON 输出新增渠道渲染字段：
    - `render_console: ![](https://...)`
    - `render_qq: [Image: https://...]`
- 已更新 `/root/.copaw/workspaces/default/active_skills/ark_t2i/SKILL.md` 使用说明，补充“自拍预设”示例。
- 实测成功生成：
  - 本地文件：`/mnt/开发/images/1773891516_image.png`
  - 公网地址：`https://co.rotes.shop/images/1773891516_image.png`
