# 2026-03-12 操作记录

## 磁盘清理与定时任务
- 用户先要求清理垃圾文件释放硬盘空间。
- 已执行一轮相对安全的清理：清理 `apt` 缓存与包列表、`/root/.npm`、`/root/.cache`，并清空 `/root/.copaw/copaw.log`、`/root/.copaw/quan2go_proxy.log`、`/root/.copaw/quan2go_proxy_raw.log`。
- 清理后磁盘使用从约 `5.3G used / 4.0G free` 变为 `5.1G used / 4.3G free`，约释放 300MB。
- 剩余主要占用是正常环境文件：`/root/.copaw/venv` 约 964MB、`/root/.local` 约 225MB；用户当时未要求继续做激进清理。

## 定时任务偏好与修正
- 用户要求设置“每天北京时间凌晨 4 点”执行垃圾清理。
- 起初误用了系统 `crontab`；随后用户明确要求改为 CoPaw 自带 `cron`，不要单独建系统级定时。
- 已撤销 root crontab 和 `/usr/local/bin/disk_cleanup.sh`。
- 已改为通过 CoPaw cron 创建：
  - 名称：`每日磁盘清理`
  - job id：`5fb6624f-38fd-49c7-8e1d-0367667871ee`
  - 调度：`0 4 * * *`
  - 时区：`Asia/Shanghai`
  - 目标：QQ 会话 `qq:72347C7DF394F00C0FB1BB40D9F74297`
- 任务要求只做安全清理，不碰 `venv`、`.local`、项目文件和用户数据。

## 经验教训
- 涉及“定时任务”时，应优先检查 CoPaw 自带 `cron` 能力（`copaw cron`），不要默认用系统 `crontab`。
- 用户对“在 CoPaw 内部实现，而不是系统外另建机制”这点很在意，后续类似需求要先确认/优先采用 CoPaw 原生方案。

## AGENTS 与记忆工作流强化
- 用户要求记忆检索规则与实际行为一致，核心是“先查记忆再回答”。
- 已强化 `AGENTS.md`：历史、偏好、配置、路径、任务状态等问题先 `memory_search`，若仍依赖当前状态，再用 `read_file` 或 `execute_shell_command` 核实。
- 已明确：禁止只凭印象回答历史问题；若记忆未命中，要直接说明。
