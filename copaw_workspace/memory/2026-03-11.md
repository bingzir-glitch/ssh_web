# 2026-03-11 操作记录

## 清理 /root/.copaw 恢复默认状态
- 用户请求清理 `/root/.copaw` 目录，删除不必要文件，恢复到默认初始状态
- 操作完成：删除了使用过程中生成的文件，包括日志文件 `copaw.log`、会话记录 `chats.json`/`sessions/`、用户记忆文件（清空 `memory/`）、虚拟环境 `venv/`、文件存储 `file_store/`/`media/`、临时配置文件 `config.json`/`jobs.json`/`server.js` 等
- 保留了所有基础框架文件（active_skills、AGENTS.md、核心文档等），目录已恢复初始状态

## 查看 /root/.copaw.secret 目录
- 用户询问 `/root/.copaw.secret` 用途及是否可删除
- 查明：该目录存储AI模型提供商配置信息，包含当前激活模型设置、各个AI平台（Doubao、OpenAI等）的API密钥等敏感配置
- 说明：若删除该目录，下次启动需要重新配置AI模型，原有API密钥会丢失
