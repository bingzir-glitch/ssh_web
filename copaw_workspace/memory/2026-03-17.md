## 2026-03-17 记录（压缩版｜不分时间段）

- HEARTBEAT 已改为轻量巡检：daily memory 只记结论/变更/TODO；无进展输出 `HEARTBEAT_OK`；默认不发 QQ，除非紧急/明确要求/不提醒会造成损失。

- 版本/目录结构认知：当时确认 CoPaw 版本为 0.0.7；文档提到 `~/.copaw/workspaces/<id>/...`，与当时机器上观察到的目录结构存在不一致点（需以当前版本与实际目录为准）。

- 记忆系统与 Embedding：记忆源为 Markdown（`MEMORY.md` 长期、`memory/YYYY-MM-DD.md` 每日追加）；Embedding 用于语义检索，常与 BM25 混合；配置通常由 `EMBEDDING_API_KEY/BASE_URL/MODEL_NAME/DIMENSIONS` 等环境变量提供。

- 火山方舟（Ark）Embedding 联调结论：
  - 控制台显示每日约 200w 免费 token（以控制台为准）；用户曾粘贴 API Key，已提醒作废重建防盗刷。
  - CoPaw/ReMe 侧期望 OpenAI 兼容 embeddings（`POST /v1/embeddings` + OpenAI 标准返回）。
  - 已验证 Ark 可用端点为多模态向量化：`POST https://ark.cn-beijing.volces.com/api/v3/embeddings/multimodal`，模型 `doubao-embedding-vision-251215`；纯文本也可调用。
  - 实测返回结构为 `{"data": {"embedding": [...]}}`（非 OpenAI 标准），向量维度 2048，示例用量 `usage.total_tokens=25`。

- 当时已写入的 embedding 配置（但因协议差异无法直接让 CoPaw 记忆向量检索跑通）：
  - `/root/.copaw/secrets/EMBEDDING_API_KEY`（复用 `/root/.copaw/secrets/ARK_API_KEY`）
  - `EMBEDDING_BASE_URL=https://ark.cn-beijing.volces.com/api/v3`
  - `EMBEDDING_MODEL_NAME=doubao-embedding-vision-251215`
  - `EMBEDDING_DIMENSIONS=2048`
  - 阻塞点：Ark 走 `/embeddings/multimodal` 且返回体不同于 OpenAI embeddings。

- 方案尝试与阻塞：
  - 已起草协议转换代理：`/root/.copaw/tools/ark_mm_embedding_proxy.py`（将 `POST /v1/embeddings` 转发到 Ark multimodal，并把响应转换为 OpenAI embeddings 结构）。
  - `127.0.0.1:18080` 被 `/root/Python/quan2go_proxy.py` 占用，代理需换端口（如 18081+）。
  - CoPaw app 监听 `127.0.0.1:8088`，但 `/openapi.json`、`/health`、`/docs` 返回前端静态 HTML；需确认真实 API 路径前缀/反代情况。
  - `copaw daemon status` 曾显示 `Memory manager: not attached`，需确认实际运行进程是否加载 embedding 配置并启用向量检索。

- TODO：
  1) 选未占用端口启动 `ark_mm_embedding_proxy.py` 并自测 `/health`、`/v1/embeddings`。
  2) 将 `EMBEDDING_BASE_URL` 指向本地代理后验证 CoPaw memory 向量检索。
  3) 或改用 Ark 的“文本向量化（OpenAI 兼容）”模型/接入点，避免维护代理。
